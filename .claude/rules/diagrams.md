---
description: Conventions for diagrams in specs
globs: ["specs/**"]
---

# Diagram Conventions

- Use **Mermaid** for all diagrams in specs.
- Use fenced ```` ```mermaid ```` blocks inline in the spec markdown. GitHub and local IDEs render these natively — no separate source files or PNGs needed.
